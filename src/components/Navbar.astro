---
interface Props {
  currentPath?: string;
}

const { currentPath = '/' } = Astro.props;

const navLinks = [
  { label: 'Home', href: '/' },
  { label: 'Blog', href: '/blog' },
  { label: 'Projects', href: '/projects' },
  { label: 'Contact', href: '/#contact' },
];
---

<nav class="fixed top-0 left-0 right-0 z-50 bg-slate-900/80 backdrop-blur-md border-b border-slate-800/50">
  <div class="max-w-7xl mx-auto px-6 py-4 flex items-center justify-between">
    {/* Logo */}
    <a href="/" class="text-2xl font-bold bg-gradient-to-r from-violet-400 to-violet-500 bg-clip-text text-transparent hover:opacity-80 transition-opacity">
      &lt;DF /&gt;
    </a>

    {/* Navigation Links */}
    <div class="flex items-center gap-8">
      <ul class="flex gap-8">
        {navLinks.map((link) => (
          <li>
            <a
              href={link.href}
              class={`text-sm font-medium transition-colors ${
                currentPath === link.href || (link.href !== '/' && currentPath.startsWith(link.href))
                  ? 'text-violet-400'
                  : 'text-slate-400 hover:text-slate-50'
              }`}
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>

      {/* Resume Button */}
      <a
        href="#"
        class="px-4 py-2 border border-violet-500 text-violet-400 rounded-md text-sm font-medium hover:bg-violet-500/10 transition-colors"
      >
        Resume
      </a>
    </div>
  </div>
</nav>
